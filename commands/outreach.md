---
description: Draft, send, hold, or cancel candidate follow-up emails.
argument-hint: "[candidate + intent — e.g. 'follow up with Mei about availability' or 'cancel the queued email']"
---

Use the `manage-outreach` skill. Request: $ARGUMENTS
