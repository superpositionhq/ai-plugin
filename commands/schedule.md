---
description: Schedule, reschedule, or handle interviews (including no-shows).
argument-hint: "[candidate/stage — e.g. 'send Diego the CTO interview link' or 'reschedule the onsite']"
---

Use the `schedule-interviews` skill. Request: $ARGUMENTS
