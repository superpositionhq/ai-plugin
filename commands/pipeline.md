---
description: Take a pipeline action on a specific candidate (move, pass, reject, withdraw, share, mark hired).
argument-hint: "[candidate + action — e.g. 'move Diego to interviewing' or 'reject — no-show']"
---

Use the `pipeline-actions` skill. Directive: $ARGUMENTS
