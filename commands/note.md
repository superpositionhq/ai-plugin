---
description: Record a confirmed fact (insight/learning) about a person.
argument-hint: "[candidate + fact — e.g. 'note that Liz needs H-1B sponsorship']"
---

Use the `record-candidate-insights` skill. Fact: $ARGUMENTS
