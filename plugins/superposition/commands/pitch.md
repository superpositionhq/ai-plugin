---
description: Pitch candidates to the client or post a client update (Slack).
argument-hint: "[who to pitch — e.g. 'pitch the top 3 in await-review to the client']"
---

Use the `pitch-to-client` skill. Request: $ARGUMENTS
