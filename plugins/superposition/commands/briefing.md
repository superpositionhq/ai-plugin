---
description: Read-only pipeline health and daily priorities.
argument-hint: "[optional scope — e.g. a job name; defaults to all active roles]"
---

Use the `pipeline-health-briefing` skill. Scope: $ARGUMENTS
