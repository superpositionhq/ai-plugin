---
description: Connect to Superposition and confirm your active org before doing recruiting work.
argument-hint: "(no args)"
---

Use the `connect-superposition` skill: confirm identity with `get_superposition_context`, pick the
active org if the user belongs to more than one, and verify access with `list_jobs`. Report the
connected user, active org, and active-job count, then suggest the next step. $ARGUMENTS
